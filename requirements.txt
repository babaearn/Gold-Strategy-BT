# =================================================================
# Dependencies for the Backtrader ATR Stop-Loss Strategy
# + XAUT Live Trading Bot (Bybit)
#
# To install all dependencies, run:
#   pip install -r requirements.txt
#
# For Railway deployment only the live-trading section is needed;
# backtest deps are optional on the server.
# =================================================================

# ── Backtest (local only) ─────────────────────────────────────
backtrader==1.9.76.123
matplotlib==3.8.4
numpy==1.26.4
Pillow==10.3.0
cycler==0.12.1
fonttools==4.51.0
kiwisolver==1.4.5
packaging==24.0
pyparsing==3.1.2
python-dateutil==2.9.0.post0
six==1.16.0

# ── Live trading bot (Railway / local) ───────────────────────
# Bybit official Python SDK (REST + WebSocket, v5 API)
pybit>=5.7.0
# Data manipulation for indicator calculations
pandas>=1.5.0
# Load .env file for local development
python-dotenv>=1.0.0